namespace MethodOverriding
{

    // this enum is no longer needed as we will use the derived classes to determine the type of shape

    public enum ShapeType
    {
        Circle,
        Rectangle
    }
}
